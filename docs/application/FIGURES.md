# Figures the research statement and the paper need

All generated by `apps/ai-service/scripts/eval_report.py` into `docs/figures/`
(PNG for slides and the statement, SVG for the paper). Regenerate after any
new run; no figure carries a hand-typed number.

| file | what it shows | used in | status |
| --- | --- | --- | --- |
| `ft1_val_curves.png/.svg` | Validation token CE vs step for every run, one panel per corpus; dashed = broken harness, solid = fixed; dotted = base model | paper §7.5′ Fig. A; statement (AI track) | generated |
| `ft2_dropout_probe.png/.svg` | Train-mode vs eval-mode loss on identical zero-init-LoRA weights (A–E), with the ln(2048) uniform-random line; the diagnosis of the July negative | paper §7.5′ Fig. B; statement (AI track) | generated |
| `ft3_pcs_voiced.png/.svg` | PCS mean and voiced fraction for base / adapter / real clips on both corpora; the "more melody, not less" pattern | paper §7.5′ Fig. C; demo close; statement | generated |
| `ft4_plateau_harvard3000.png/.svg` | Harvard raw 3000-step run: val and test CE per checkpoint, best at 2750, plateau by ~2500 | paper §7.5′ Fig. D | generated |
| `ft5_train_loss_broken_vs_fixed.png/.svg` | Smoothed train loss: broken runs settle above ln(2048); fixed runs sit at 2–4 nats | paper appendix; statement backup | generated |
| `ft6_denoise_effect.png/.svg` | DeepFilterNet effect on the clips: ~15 dB removed, waveform correlation 0.41 (p10–p90 0.24–0.64) | paper §4 methods caveat | generated |
| `ft7_scale_val_curves.png/.svg` | Validation token CE per checkpoint for the medium and large scale runs, each against its own base (dotted); both flatten by ~2,500 steps | demo page (Results); paper scaling section | generated by `build_demo_page.py` |
| `fig1_cents_histograms.png`, `fig2_aligned_histogram.png`, `fig3_era_comparison.png`, `fig4_embedding_map.png` | The paper's existing intonation, alignment, era and embedding figures (from `analyze_corpus.py` / `analyze_embeddings.py`, July) | paper §§5–7.3 | pre-existing, unchanged |

Still needed (not generated, listed so nothing is forgotten):

| figure | why | how |
| --- | --- | --- |
| Listening-study results (oud and Harvard A/B, preference + comments) | the only evidence that matters more than CE | after Khalid (and, later, Somali musicians) listen: tally preferences per pair, plot as paired bars |
| Spectrogram pair: base vs adapter for one prompt (oud) | one image for the statement's preservation track | `librosa.display.specshow` on two API outputs with the same seed; label as model output |
| Pipeline / system diagram: archive → tokens → LoRA → API → demo, with the rights boundary drawn | statement (preservation track) and paper §4 | draw by hand (Figma/Excalidraw); the boundary line is the point |
