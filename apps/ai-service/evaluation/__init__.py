"""Evaluation harness for the Somali music models and dataset baselines.

Pure metric implementations live here (WER, confusion-derived P/R/F1, raw
pitch accuracy) so the numbers in the ISMIR paper are computed by code this
repo unit-tests, not by whichever metrics library happens to be installed.
"""
