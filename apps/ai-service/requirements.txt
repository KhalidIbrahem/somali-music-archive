# Somali Music AI service — Python 3.11 (ARCHITECTURE.md §5, §10).
# Pin ranges are conservative; lock exact versions in requirements.lock at deploy.

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
gunicorn==22.0.0
pydantic==2.7.4
pydantic-settings==2.3.4
httpx==0.27.0
python-dotenv==1.0.1

# ── Job queue + observability (transcription worker, ARCHITECTURE.md §8) ─────
celery==5.4.0
redis==5.0.7
sentry-sdk==2.7.1

# ── Audio / ML (heavy — installed on the GPU inference image, not needed for the
#    pure scale-mapping unit tests). See ARCHITECTURE.md §10. ──
numpy==1.26.4
librosa==0.10.2.post1
soundfile==0.12.1
crepe==0.0.15
torch==2.3.1
transformers==4.42.3
# openai-whisper is pulled from git per its own install guidance:
#   pip install git+https://github.com/openai/whisper.git
