"""Offline research pipeline scripts (Harvard corpus processing, model training).

These are batch entry points, not part of the FastAPI service. They are kept
importable as a package so their pure helper functions are unit-testable.
"""
